version: '3.7'

services:
  dev:
    build:
      dockerfile: Dockerfile
      context: .
      target: dev
      args:
        PHP_VERSION: $PHP_VERSION
        THECODINGMACHINE_VERSION: $THECODINGMACHINE_VERSION
        BUILD_THECODINGMACHINE_VERSION: $BUILD_THECODINGMACHINE_VERSION
        WORDPRESS_VERSION: $WORDPRESS_VERSION
        WORDPRESS_DEV_VERSION: $WORDPRESS_DEV_VERSION
        WOOCOMMERCE_VERSION: $WOOCOMMERCE_VERSION
    ports:
      - 8888:80
    restart: unless-stopped
    environment:
      WORDPRESS_DB_HOST: db-test
      WORDPRESS_DB_USER: exampleuser
      WORDPRESS_DB_PASSWORD: examplepass
      WORDPRESS_DB_NAME: exampledb
      WORDPRESS_ADMIN_USER: admin
      WORDPRESS_ADMIN_PASSWORD: examplepass
      COPY_TO_MOUNT: 1
      PHP_IDE_CONFIG: serverName=docker
    volumes:
      - ./mount/:/var/www/html/mount/:rw
      - .:/var/www/html/wordpress-develop/src/wp-content/plugins/storekeeper-for-woocommerce:rw
    depends_on:
      db:
        condition: service_healthy

  db:
    image: mysql:5.7
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: exampledb
      MYSQL_USER: exampleuser
      MYSQL_PASSWORD: examplepass
      MYSQL_ROOT_PASSWORD: somerootpassword
    volumes:
      - db:/var/lib/mysql
    healthcheck:
      test: [ "CMD", "mysqladmin" ,"ping", "-h", "localhost" ]
      timeout: 20s
      retries: 10

  test:
    build:
      dockerfile: Dockerfile
      context: .
      target: test
      args:
        PHP_VERSION: $PHP_VERSION
        THECODINGMACHINE_VERSION: $THECODINGMACHINE_VERSION
        BUILD_THECODINGMACHINE_VERSION: $BUILD_THECODINGMACHINE_VERSION
        WORDPRESS_VERSION: $WORDPRESS_VERSION
        WOOCOMMERCE_VERSION: $WOOCOMMERCE_VERSION
        WORDPRESS_DEV_VERSION: $WORDPRESS_DEV_VERSION
    environment:
      WORDPRESS_DB_HOST: db-test
      WORDPRESS_DB_USER: exampleuser
      WORDPRESS_DB_PASSWORD: examplepass
      WORDPRESS_DB_NAME: exampledb
      WORDPRESS_ADMIN_USER: admin
      WORDPRESS_ADMIN_PASSWORD: examplepass
      COPY_TO_MOUNT: ${COPY_TO_MOUNT:-1}
      PHP_IDE_CONFIG: serverName=docker
    stdin_open: true
    tty: true
    volumes:
      - ./mount/:/usr/src/app/mount/:rw
    depends_on:
      db-test:
        condition: service_healthy

  db-test:
    image: mysql:5.7
    restart: always
    healthcheck:
      test: [ "CMD", "mysqladmin" ,"ping", "-h", "localhost" ]
      timeout: 20s
      retries: 10
    environment:
      MYSQL_DATABASE: exampledb
      MYSQL_USER: exampleuser
      MYSQL_PASSWORD: examplepass
      MYSQL_ROOT_PASSWORD: somerootpassword
    volumes:
      - db-test:/var/lib/mysql

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    restart: unless-stopped
    links:
      - db:db
      - db-test:db-test
    ports:
      - 8881:80
    environment:
      MYSQL_USERNAME: root
      MYSQL_ROOT_PASSWORD: somerootpassword

volumes:
  db:
  db-test:
  test-run:
